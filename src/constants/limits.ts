/** Upper bound for a single logged activity amount (e.g. km, kWh, servings). */
export const MAX_ACTIVITY_VALUE = 100000;

/** Upper bound for a user-defined CO2 goal limit in kilograms. */
export const MAX_GOAL_LIMIT_KG = 100000;
